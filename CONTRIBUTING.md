# Contributing

1. Agent in `registry.py` + `guides.py`
2. Pick model: large / ministral / devstral / pixtral
3. Vision agents: `requires_image=True`
4. French prompts, JSON output
5. No secrets in commits